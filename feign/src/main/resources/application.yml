spring:
  application:
    name: feign-comsumer
server:
  port: 8800
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8700/eureka/,http://locakhost:8701/eureka/,http://locakhost:8702/eureka/

feign:
  hystrix:
    enabled: true