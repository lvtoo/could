spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/lvtoo/could/
          search-paths: config-repo
          username: username
          password: password
server:
  port: 8799

